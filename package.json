{
  "name": "inventario-web",
  "version": "1.0.0",
  "description": "Inventario Web App",
  "private": true,
  "scripts": {
    "dev": "live-server --host=0.0.0.0 --port=5500 --open=./index.html --watch=."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "dotenv": "^17.4.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
